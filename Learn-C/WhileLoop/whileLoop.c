#include<stdio.h>

int main(){
    int i = 0;
    while (i < 5)
    {
       printf("Student no : %d\n" , i);
       i++; // the loop will run over and over again, as long as a var(i) is less than 5
    }
    
}

